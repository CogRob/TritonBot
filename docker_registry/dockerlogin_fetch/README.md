# Image for `dockerlogin` on Fetch/Freight

A Docker Image to use with `dockerlogin` on Fetch/Freight Robots.

## VNC
* Use `x11vnc -create` with `-passwd` to start VNC server.
* In the terminal, use `nohup startlxde &` and `disown` to start LXDE.
